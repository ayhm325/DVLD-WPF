<Window x:Class="Presentation.Views.Windows.LocalApplicationDetailsWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Presentation.Views.Controls"
        mc:Ignorable="d"
        Title="Local Driving License Application Details"
        Height="797" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F0F2F5">
    <!-- نفس لون الخلفية المستخدم داخل الكنترولات ليتمازجا -->

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Local Driving License Application Details"
                   FontSize="26"
                   FontWeight="SemiBold"
                   Foreground="#333333"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- Driving License Application Info -->
        <!-- لم نضع Border هنا لأن الكنترول يحتوي بالفعل على تصميمه الخاص -->
        <!-- أضفنا هامشاً بسيطاً من الأعلى للفصل بينه وبين العنوان -->
        <controls:DrivingLicenseApplicationInfo Grid.Row="1" 
                                                 Margin="0,10,0,0"/>

        <!-- Application Basic Info -->
        <!-- هذا الكنترول يحتوي على Margin داخلي، لذا نضعه مباشرة وسيمتلأ المساحة المتبقية -->
        <controls:ApplicationBasicInfo Grid.Row="2"/>

    </Grid>
</Window>