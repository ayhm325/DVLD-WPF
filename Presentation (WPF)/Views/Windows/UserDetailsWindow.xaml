<Window x:Class="Presentation.Views.Windows.UserDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:controls="clr-namespace:Presentation.Views.Controls"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="User Details" 
        Width="950"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundColor}">

    <!-- تمت إزالة الستايل المحلي للأزرار لاستخدام الستايل العام -->

    <!-- الحاوية الرئيسية -->
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 1. بطاقة تفاصيل المستخدم (Personal Info) -->
        <controls:InformationPerson Grid.Row="0" 
                                Person="{Binding Person}"/>

        <!-- 2. بطاقة حالة المستخدم (User Card) مع مسافة للفصل -->
        <controls:UserCard Grid.Row="1" 
                    DataContext="{Binding}" 
                    Margin="0,25,0,0"/>

        <!-- 3. زر الإغلاق -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,0,0">
            <Button Content="Close" 
                    Width="120" 
                    Height="40" 
                    Click="CloseButton_Click"
                    Style="{StaticResource PrimaryButtonStyle}"/>
        </StackPanel>

    </Grid>
</Window>