<Window x:Class="Presentation.Views.Windows.ReplacementDamagedLicense"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Presentation.Views.Windows"
        xmlns:controls="clr-namespace:Presentation.Views.Controls"
        mc:Ignorable="d"
        Title="Replacement Damaged License"
        Height="680" Width="840"
        WindowStartupLocation="CenterScreen"        
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- تدرج الهيدر الموحد -->
        <LinearGradientBrush x:Key="HeaderBgGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#042F2E" Offset="0"/>
            <GradientStop Color="#0A0A0B" Offset="1"/>
        </LinearGradientBrush>

        <!-- ستايل زر الإغلاق -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource SecondaryTextColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1F2937"/>
                                <Setter Property="Foreground" Value="{StaticResource ErrorColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Ghost Button -->
        <Style x:Key="GhostButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#042F2E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#1A1A1E"/>
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#6B7280"/>
                                <Setter Property="Opacity" Value="0.5"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Radio Button ستايل -->
        <Style x:Key="HeaderRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource SecondaryTextColor}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid Margin="0" VerticalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <!-- الدائرة الخارجية -->
                            <Ellipse Grid.Column="0" 
                                     Width="16" Height="16"
                                     Stroke="{TemplateBinding Foreground}"
                                     StrokeThickness="1.5"
                                     Fill="Transparent"
                                     VerticalAlignment="Center"
                                     Margin="0,0,8,0"/>
                            <!-- النقطة الداخلية -->
                            <Ellipse Grid.Column="0" 
                                     x:Name="innerDot"
                                     Width="8" Height="8"
                                     Fill="{TemplateBinding Foreground}"
                                     VerticalAlignment="Center"
                                     Opacity="0"/>
                            <!-- النص -->
                            <ContentPresenter Grid.Column="1" 
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
                                <Setter TargetName="innerDot" Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- الحاوية الرئيسية -->
    <Border Background="{StaticResource BackgroundColor}" 
            BorderBrush="{StaticResource BorderColor}" 
            BorderThickness="1" 
            CornerRadius="16" 
            Margin="15">

        <Border.Effect>
            <DropShadowEffect Color="Black" Direction="270" ShadowDepth="0" BlurRadius="30" Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Header -->
                <RowDefinition Height="*"/>
                <!-- Scrollable Content -->
                <RowDefinition Height="Auto"/>
                <!-- Buttons -->
            </Grid.RowDefinitions>

            <!-- ================= الهيدر الموحد (معدل) ================= -->
            <Border Grid.Row="0" Background="{StaticResource HeaderBgGradient}" 
                    CornerRadius="16,16,0,0" 
                    Padding="25,18,25,18">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                    <!-- أيقونة البطاقة (تم تصحيح اللون ليكون فاتحاً وواضحاً) -->
                    <Border Width="38" Height="38" CornerRadius="10" 
                            Background="{StaticResource PrimaryColor}" 
                            Opacity="0.15"
                            Margin="0,0,14,0"
                            Padding="8"
                            VerticalAlignment="Center">
                        <Path Data="M19,12H21V14H19V12M19,8H21V10H19V8M3,20H17V4H3A2,2 0 0,0 1,6V18A2,2 0 0,0 3,20Z" 
                              Stroke="{StaticResource TextColor}" StrokeThickness="1.6" 
                              Fill="Transparent" Stretch="Uniform" Height="20" Width="20"/>
                    </Border>

                    <!-- العنوان والخيارات مجتمعين في سطر واحد -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                        <!-- عنوان النافذة الرئيسي -->
                        <TextBlock Text="License Replacement" 
                                   FontSize="18" FontWeight="SemiBold" 
                                   Foreground="{StaticResource TextColor}" 
                                   VerticalAlignment="Center"
                                   Margin="0,0,12,0"/>

                        <!-- فاصل بسيط -->
                        <TextBlock Text="-" 
                                   FontSize="18" FontWeight="Light" 
                                   Foreground="{StaticResource SecondaryTextColor}" 
                                   VerticalAlignment="Center"
                                   Margin="0,0,15,0"/>

                        <!-- خيارات نوع الاستبدال (خط أوضح وأقرب للعنوان) -->
                        <RadioButton Content="Damaged License"
                                     Style="{StaticResource HeaderRadioButtonStyle}"
                                     GroupName="ReplacementType"
                                     IsChecked="True"
                                     FontSize="14" FontWeight="SemiBold"
                                     Margin="0,0,20,0"
                                     Command="{Binding SelectDamagedCommand}"/>
                        <RadioButton Content="Lost License"
                                     Style="{StaticResource HeaderRadioButtonStyle}"
                                     GroupName="ReplacementType"
                                     IsChecked="False"
                                     FontSize="14" FontWeight="SemiBold"
                                     Command="{Binding SelectLostCommand}"/>

                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- ================= المحتوى ================= -->
            <ScrollViewer Grid.Row="1" 
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Margin="20,15,20,20">

                <StackPanel>

                    <!-- Driver License Info Card -->
                    <controls:DriverLicenseInfo
                            LicenseIdText="{Binding LicenseIdText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            SearchCommand="{Binding SearchCommand}"
                            License="{Binding LicenseInfo}"/>

                    <!-- Application New License Info Card -->
                    <controls:ApplicationInfoForLicenseReplacement Application="{Binding ReplacementInfo}"/>

                </StackPanel>

            </ScrollViewer>

            <!-- ================= شريط الأزرار السفلي ================= -->
            <Border Grid.Row="2" Background="{StaticResource BackgroundColor}" 
                    Padding="20,15,25,20" 
                    CornerRadius="0,0,16,16"
                    Margin="-1,0,-1,-1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left Side -->
                    <StackPanel Grid.Column="0"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">

                        <Button Content="Show Licenses History"
                                Style="{StaticResource GhostButtonStyle}"
                                Command="{Binding ShowLicensesHistoryCommand}"
                                IsEnabled="{Binding IsLicenseIssued}"/>

                        <Rectangle Width="1" Height="20"
                                   Fill="{StaticResource BorderColor}"
                                   Margin="12,0"
                                   VerticalAlignment="Center"/>

                        <Button Content="Show Licenses Info"
                                Style="{StaticResource GhostButtonStyle}"
                                Command="{Binding ShowLicensesInfoCommand}"
                                IsEnabled="{Binding IsLicenseIssued}"/>

                    </StackPanel>

                    <!-- Right Side -->
                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <Button Content="Issue"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Width="120"
                                Margin="0,0,15,0"
                                Command="{Binding IssueCommand}"/>
                        <Button Content="Close"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Width="120"
                                Click="CloseButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

        </Grid>
    </Border>
</Window>