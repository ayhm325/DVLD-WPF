<Window x:Class="Presentation.Views.Windows.Applications.EditApplicationTypeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
        mc:Ignorable="d"
        Title="Update Application Type" Height="450" Width="500"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Update Application Type" FontSize="25" FontWeight="Bold" 
                   Foreground="#2C3E50" Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10">
            <StackPanel Orientation="Horizontal" Width="80">
                <materialDesign:PackIcon Kind="Identifier" Foreground="Green" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBlock Text="ID:" FontWeight="Bold" FontSize="15"/>
            </StackPanel>
            <TextBlock Text="{Binding CurrentApplicationType.ApplicationTypeId}" 
                       FontWeight="Bold" FontSize="15" Foreground="#333" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,10">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <materialDesign:PackIcon Kind="TextBoxEditOutline" Foreground="Green" Margin="0,0,5,0"/>
                <TextBlock Text="Title:" FontSize="15" FontWeight="Bold"/>
            </StackPanel>
            <TextBox Text="{Binding CurrentApplicationType.ApplicationTypeTitle, UpdateSourceTrigger=PropertyChanged}" 
                     Height="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,10">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <materialDesign:PackIcon Kind="Cash100" Foreground="Green" Margin="0,0,5,0"/>
                <TextBlock Text="Fees:" FontSize="15" FontWeight="Bold"/>
            </StackPanel>
            <TextBox Text="{Binding CurrentApplicationType.ApplicationTypeFees, StringFormat='{}{0:F2}', UpdateSourceTrigger=PropertyChanged}" 
                     Height="30" VerticalAlignment="Center" Padding="5,0,0,0" AcceptsReturn="False" />
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,20,0,0">
            <Button Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    Width="100" Height="35" Margin="0,0,10,0" Padding="5,0,0,0" Style="{StaticResource PrimaryButtonStyle}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ContentSave" Margin="0,0,5,0"/>
                    <TextBlock Text="Save"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding CloseCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    Width="100" Height="35" Style="{StaticResource PrimaryButtonStyle}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Close" Margin="0,0,5,0"/>
                    <TextBlock Text="Close"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Window>