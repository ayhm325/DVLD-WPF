<Window x:Class="Presentation.Views.Windows.NewInternationalLicenseApplicationWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Presentation.Views.Windows"
        xmlns:controls="clr-namespace:Presentation.Views.Controls"
        mc:Ignorable="d"
        Title="New International License Application"
        Height="620" Width="800"
        MinHeight="500" MinWidth="750"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <!-- Ghost Button فقط – غير موجود في الستايل العام -->
        <Style x:Key="GhostButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#1E1B4B"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#312E81"/>
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- ═══════════ ScrollViewer ═══════════ -->
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ═══════════ Title ═══════════ -->
            <TextBlock Grid.Row="0"
                       Text="New International License Application"
                       Style="{StaticResource HeaderStyle}"
                       Margin="0,0,0,20"/>

            <!-- ═══════════ Driver License Info Card ═══════════ -->
            <Border Grid.Row="1"
                    Style="{StaticResource CardStyle}"
                    Padding="2"
                    Margin="0,0,0,16">
                <controls:DriverLicenseInfo/>
            </Border>

            <!-- ═══════════ Application Basic Info Card ═══════════ -->
            <Border Grid.Row="2"
                    Style="{StaticResource CardStyle}"
                    Padding="2">
                <controls:ApplicationBasicInfo/>
            </Border>

            <!-- ═══════════ Bottom Bar ═══════════ -->
            <Grid Grid.Row="3" Margin="0,20,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Left Side -->
                <StackPanel Grid.Column="0"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <Button Content="Show Licenses History"
                            Style="{StaticResource GhostButtonStyle}"/>
                    <Rectangle Width="1" Height="20"
                               Fill="{StaticResource BorderColor}"
                               Margin="12,0"
                               VerticalAlignment="Center"/>
                    <Button Content="Show Licenses Info"
                            Style="{StaticResource GhostButtonStyle}"/>
                </StackPanel>

                <!-- Right Side -->
                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <Button Content="Issue"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Margin="0,0,10,0"/>
                    <Button Content="Close"
                            Style="{StaticResource SecondaryButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</Window>