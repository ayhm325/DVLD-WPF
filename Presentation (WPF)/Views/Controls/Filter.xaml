<UserControl x:Class="Presentation.Views.Controls.Filter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignWidth="800">

    <Border Style="{StaticResource CardStyle}" Padding="15" CornerRadius="12">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Filter:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,15,0"/>
            <TextBlock Grid.Column="1" Text="Find By:" VerticalAlignment="Center" Margin="0,0,10,0"/>

            <Border Grid.Column="2" CornerRadius="8" BorderBrush="#CBD5E1" BorderThickness="1" 
                    Background="White" Margin="0,0,10,0" Height="38" Width="140">
                <ComboBox SelectedIndex="{Binding SelectedFilterIndex}" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="10,0">
                    <ComboBoxItem Content="Person ID"/>
                    <ComboBoxItem Content="National No"/>
                </ComboBox>
            </Border>

            <Border Grid.Column="3" CornerRadius="8" BorderBrush="#CBD5E1" BorderThickness="1" 
                    Background="White" Margin="0,0,10,0" Height="38">
                <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" 
                         Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="10,0"/>
            </Border>

            <Button Grid.Column="4" Content="Search" Style="{StaticResource PrimaryButtonStyle}" 
                    Width="90" Height="38" Margin="0,0,5,0" Command="{Binding SearchCommand}"/>

            <Button Grid.Column="5" Content="+ Add Person" Style="{StaticResource PrimaryButtonStyle}" 
                    Width="110" Height="38" Background="OliveDrab" Command="{Binding AddPersonCommand}"/>
        </Grid>
    </Border>
</UserControl>