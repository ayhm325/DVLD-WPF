<UserControl x:Class="Presentation.Views.Controls.UserCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="400">

    <UserControl.Resources>
        <!-- ألوان شارة الحالة (تطابق ألوان التفعيل في الستايل العام) -->
        <SolidColorBrush x:Key="ActiveGreenBg" Color="#064E3B"/>
        <SolidColorBrush x:Key="ActiveGreenText" Color="#34D399"/>

        <!-- خلفية أيقونة المعلومات (تطابق النمط الموحد) -->
        <SolidColorBrush x:Key="IconBgBrush" Color="#042F2E"/>
    </UserControl.Resources>

    <!-- البطاقة الرئيسية باستخدام CardStyle -->
    <Border Style="{StaticResource CardStyle}" Padding="20,15" Margin="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <!-- الأيقونة -->
                <ColumnDefinition Width="*"/>
                <!-- معلومات المستخدم -->
                <ColumnDefinition Width="Auto"/>
                <!-- الحالة -->
            </Grid.ColumnDefinitions>

            <!-- عمود الأيقونة (مربّع مستدير – النمط الموحد) -->
            <Border Grid.Column="0" 
                    Width="45" Height="45" 
                    CornerRadius="11" 
                    Background="{StaticResource IconBgBrush}" 
                    VerticalAlignment="Center"
                    Padding="10">
                <materialDesign:PackIcon Kind="Account" 
                                        Foreground="{StaticResource PrimaryColor}" 
                                        Width="24" Height="24" 
                                        VerticalAlignment="Center" 
                                        HorizontalAlignment="Center"/>
            </Border>

            <!-- عمود الاسم -->
            <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="User" 
                           FontSize="10" 
                           Foreground="{StaticResource SecondaryTextColor}" 
                           FontWeight="Medium" 
                           Margin="0,0,0,2"/>
                <TextBlock Text="{Binding UserName}" 
                           FontSize="16" 
                           FontWeight="SemiBold" 
                           Foreground="{StaticResource TextColor}" 
                           TextTrimming="CharacterEllipsis"/>
            </StackPanel>

            <!-- عمود الحالة (شارة مستطيلة – النمط الموحد) -->
            <Border Grid.Column="2" 
                    Background="{StaticResource ActiveGreenBg}" 
                    Padding="12,6" 
                    CornerRadius="6" 
                    Margin="15,0,0,0" 
                    VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="6" Height="6" Fill="{StaticResource ActiveGreenText}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding IsActive}" 
                               FontSize="12" 
                               FontWeight="SemiBold" 
                               Foreground="{StaticResource ActiveGreenText}" 
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

        </Grid>
    </Border>

</UserControl>