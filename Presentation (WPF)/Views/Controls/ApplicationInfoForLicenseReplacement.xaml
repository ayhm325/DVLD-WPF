<UserControl x:Class="Presentation.Views.Controls.ApplicationInfoForLicenseReplacement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.Views.Controls"
             mc:Ignorable="d" 
             x:Name="Root"
             HorizontalContentAlignment="Stretch"
             d:DesignHeight="250" d:DesignWidth="800">

    <UserControl.Resources>
        <LinearGradientBrush x:Key="HeaderBgGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#042F2E" Offset="0"/>
            <GradientStop Color="#0A0A0B" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="FieldLabel" TargetType="TextBlock" BasedOn="{StaticResource LabelStyle}">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0"/>
        </Style>

        <Style x:Key="FieldValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource CardStyle}" Padding="25" Margin="20,10,20,20" HorizontalAlignment="Stretch">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ═══════════════ HEADER ═══════════════ -->
            <Border Grid.Row="0" Background="{StaticResource HeaderBgGradient}" 
                    CornerRadius="12" Padding="18,14,18,14" Margin="-25,-25,-25,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="38" Height="38" CornerRadius="10" 
                                Background="{StaticResource PrimaryColor}" 
                                Opacity="0.15"
                                VerticalAlignment="Center" Margin="0,0,14,0"
                                Padding="8">
                            <materialDesign:PackIcon Kind="License" 
                                                    Foreground="{StaticResource PrimaryColor}" 
                                                    Width="22" Height="22" 
                                                    VerticalAlignment="Center" 
                                                    HorizontalAlignment="Center"/>
                        </Border>

                        <TextBlock Text="License Replacement Application Info" 
                                   FontSize="18" 
                                   FontWeight="SemiBold" 
                                   Foreground="{StaticResource TextColor}" 
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- فاصل -->
            <Border Grid.Row="0" Height="1" VerticalAlignment="Bottom" Margin="0,0,0,20" Background="{StaticResource BorderColor}"/>

            <!-- ═══════════════ DATA GRID ═══════════════ -->
            <Grid Grid.Row="1" Margin="0,4,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- ═══ Row 0: L.R.Application ID | Replaced License ID | Application Date ═══ -->
                <StackPanel Grid.Column="0" Margin="0,0,0,15">
                    <TextBlock Text="L.R.APPLICATION ID" Style="{StaticResource FieldLabel}"/>
                    <TextBlock Text="{Binding Application.ReplacementApplicationID, ElementName=Root}" 
                               Style="{StaticResource FieldValue}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,0,15">
                    <TextBlock Text="REPLACED LICENSE ID" Style="{StaticResource FieldLabel}"/>
                    <TextBlock Text="{Binding Application.ReplacementLicenseID, ElementName=Root}" 
                               Style="{StaticResource FieldValue}"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="0,0,0,15">
                    <TextBlock Text="APPLICATION DATE" Style="{StaticResource FieldLabel}"/>
                    <TextBlock Text="{Binding Application.ApplicationDate, ElementName=Root, StringFormat='{}{0:dd/MMM/yyyy}'}" 
                               Style="{StaticResource FieldValue}"/>
                </StackPanel>

                <!-- ═══ Row 1: Old License ID | Application Fees | Created By ═══ -->
                <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,5,0,0">
                    <TextBlock Text="OLD LICENSE ID" Style="{StaticResource FieldLabel}"/>
                    <TextBlock Text="{Binding Application.OldLicenseID, ElementName=Root}" 
                               Style="{StaticResource FieldValue}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Grid.Row="1" Margin="0,5,0,0">
                    <TextBlock Text="APPLICATION FEES" Style="{StaticResource FieldLabel}"/>
                    <TextBlock Text="{Binding Application.ApplicationFees, ElementName=Root}" 
                               Style="{StaticResource FieldValue}"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Grid.Row="1" Margin="0,5,0,0">
                    <TextBlock Text="CREATED BY" Style="{StaticResource FieldLabel}"/>
                    <TextBlock Text="{Binding Application.CreatedByUserName, ElementName=Root}" 
                               Style="{StaticResource FieldValue}"/>
                </StackPanel>

            </Grid>
        </Grid>
    </Border>
</UserControl>