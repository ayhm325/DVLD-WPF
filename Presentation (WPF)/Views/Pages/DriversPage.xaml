<Page xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" x:Class="Presentation.Views.Pages.DriversPage" mc:Ignorable="d" d:DesignHeight="750" d:DesignWidth="1100" Title="DriversPage" Background="{StaticResource BackgroundColor}">

    <Page.Resources>
        <Style x:Key="FilterTextBoxStyle" TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Width" Value="220" />
            <Setter Property="Height" Value="36" />
            <Setter Property="Margin" Value="10,0,0,0" />
        </Style>

        <Style TargetType="{x:Type DataGridRow}">
            <Setter Property="Height" Value="46" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Tag" Value="{Binding DataContext,RelativeSource={RelativeSource AncestorType={x:Type Page}}}" />
            <Style.Triggers>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsMouseOver" Value="True" />
                        <Condition Property="IsSelected" Value="False" />
                    </MultiTrigger.Conditions>
                    <Setter Property="Background" Value="{StaticResource SurfaceHoverColor}" />
                </MultiTrigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource SelectionBackgroundColor}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        </Page.Resources>

    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Padding="22" Margin="0,0,0,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="DRIVERS" FontSize="{StaticResource FontSizeCaption}" FontWeight="{StaticResource FontWeightSemiBold}" Foreground="{StaticResource PrimaryColor}" />
                    <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                        <TextBlock Text="Drivers Management" Style="{StaticResource HeaderStyle}" />
                        <TextBlock Text="  •  " Style="{StaticResource HeaderStyle}" Foreground="{StaticResource TextMutedColor}" />
                        <TextBlock Text="{Binding DriversCount}" Style="{StaticResource HeaderStyle}" Foreground="{StaticResource PrimaryColor}" />
                    </StackPanel>
                    <TextBlock Text="Review drivers and their active licenses" Foreground="{StaticResource TextMutedColor}" FontSize="{StaticResource FontSizeCaption}" Margin="0,4,0,0" />
                </StackPanel>

                <Border Grid.Column="1" Background="{StaticResource PrimarySoftColor}" BorderBrush="{StaticResource PrimaryLightColor}" BorderThickness="1" CornerRadius="{StaticResource RadiusPill}" Padding="12,7" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="7" Height="7" Fill="{StaticResource SuccessColor}" Margin="0,0,7,0" VerticalAlignment="Center" />
                        <TextBlock Text="Driver records" Foreground="{StaticResource TextSecondaryColor}" FontSize="{StaticResource FontSizeCaption}" FontWeight="{StaticResource FontWeightSemiBold}" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="12" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="FILTER" FontSize="{StaticResource FontSizeCaption}" FontWeight="{StaticResource FontWeightSemiBold}" Foreground="{StaticResource TextMutedColor}" VerticalAlignment="Center" Margin="0,0,14,0" />

                <ComboBox x:Name="cmbFilterBy" SelectedIndex="0" Width="160" Height="36">
                    <ComboBoxItem Content="None" IsSelected="True" />
                    <ComboBoxItem Content="Full Name" />
                    <ComboBoxItem Content="Driver ID" />
                    <ComboBoxItem Content="Person ID" />
                </ComboBox>

                <TextBox x:Name="txtFilterValue">
                    <TextBox.Style>
                        <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource FilterTextBoxStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=cmbFilterBy,Path=Text}" Value="None">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0" ClipToBounds="True">
            <DataGrid x:Name="dgDrivers" ItemsSource="{Binding Drivers}" SelectedItem="{Binding SelectedDriver}" Margin="0">
                <DataGrid.RowStyle>
                    <Style TargetType="{x:Type DataGridRow}" BasedOn="{StaticResource DataGridRowStyle}">
                            <Setter Property="Tag" Value="{Binding DataContext,RelativeSource={RelativeSource AncestorType={x:Type Page}}}" />
                            <Setter Property="ContextMenu">
                            <Setter.Value>
                                <ContextMenu>
                                    <MenuItem Header="Show Person Info" Command="{Binding PlacementTarget.Tag.ShowPersonInfoCommand,RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}" CommandParameter="{Binding PlacementTarget.DataContext,RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}"><MenuItem.Icon><materialDesign:PackIcon Kind="AccountCircle" Width="18" Height="18" Foreground="{StaticResource PrimaryColor}" /></MenuItem.Icon></MenuItem>
                                    <MenuItem Header="Show Person License History" Command="{Binding PlacementTarget.Tag.ShowLicenseHistoryCommand,RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}" CommandParameter="{Binding PlacementTarget.DataContext,RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}"><MenuItem.Icon><materialDesign:PackIcon Kind="History" Width="18" Height="18" Foreground="{StaticResource SuccessColor}" /></MenuItem.Icon></MenuItem>
                                </ContextMenu>
                            </Setter.Value>
                        </Setter>
                        <EventSetter Event="MouseRightButtonDown" Handler="DataGridRow_MouseRightButtonDown" />
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Driver ID" Binding="{Binding DriverId}" Width="90" />
                    <DataGridTextColumn Header="Person ID" Binding="{Binding PersonId}" Width="90" />
                    <DataGridTextColumn Header="National No" Binding="{Binding NationalNo}" Width="130" />
                    <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="*" />
                    <DataGridTextColumn Header="Date" Binding="{Binding CreatedDate,StringFormat='yyyy/MM/dd'}" Width="115" />
                    <DataGridTextColumn Header="Active Licenses" Binding="{Binding ActiveLicenses}" Width="125" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>