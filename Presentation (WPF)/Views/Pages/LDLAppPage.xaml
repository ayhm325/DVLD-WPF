<Page x:Class="Presentation.Views.Pages.LDLAppPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="clr-namespace:Presentation.Views.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="Driving License Applications"
      Background="{StaticResource BackgroundColor}">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Local Driving License Applications " 
                       Style="{StaticResource HeaderStyle}"/>
            <TextBlock Text="{Binding Applications.Count, StringFormat='({0})'}" 
                       Style="{StaticResource HeaderStyle}" 
                       Foreground="#64748B" 
                       Margin="5,0,0,0"/>
        </StackPanel>

        <!-- Filter Section -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="Filter by:" 
                           Style="{StaticResource LabelStyle}" 
                           VerticalAlignment="Center" 
                           Margin="0,0,10,0"/>

                <ComboBox Width="150" 
                          Margin="0,0,10,0" 
                          SelectedIndex="0"
                          SelectedItem="{Binding SelectedFilter, Mode=TwoWay}"
                          VerticalAlignment="Center">
                    <ComboBoxItem Content="Full Name"/>
                    <ComboBoxItem Content="National No."/>
                </ComboBox>

                <TextBox Width="220" 
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Add New Button -->
            <Button Grid.Column="2"
                    Content="+ Add New" 
                    Style="{StaticResource PrimaryButtonStyle}" 
                    Command="{Binding AddNewCommand}" 
                    Width="140" 
                    HorizontalAlignment="Right" />
        </Grid>

        <!-- DataGrid Section -->
        <DataGrid Grid.Row="3" 
                  ItemsSource="{Binding Applications}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  GridLinesVisibility="Horizontal"
                  BorderThickness="0"
                  Background="White"
                  RowBackground="White"
                  AlternatingRowBackground="#F8FAFC"
                  HeadersVisibility="Column"
                  HorizontalGridLinesBrush="{StaticResource BorderColor}">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="FontSize" Value="13"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Height" Value="40"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F1F5F9"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Padding" Value="10,0"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#E0E7FF"/>
                            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ApplicationID}" Width="50"/>
                <DataGridTextColumn Header="Driving Class" Binding="{Binding DrivingClass}" Width="*"/>
                <DataGridTextColumn Header="National No." Binding="{Binding NationalNo}" Width="120"/>
                <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="Date" Binding="{Binding ApplicationDate, StringFormat=yyyy/MM/dd}" Width="100"/>
                <DataGridTextColumn Header="Passed Test" Binding="{Binding PassedTest}" Width="100"/>
                <DataGridTextColumn Header="Status" Binding="{Binding StatusText}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>